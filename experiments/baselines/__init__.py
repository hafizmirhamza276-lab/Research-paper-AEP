"""The systems under comparison (PAPER_ROADMAP.md section 3.3).

Six systems, one workload driver, one connector, one ground-truth oracle. What
differs between them is only what the roadmap says differs -- see
``contract.py`` for the descriptor table that makes each row's claim about
itself a machine-readable fact rather than a comment.
"""
