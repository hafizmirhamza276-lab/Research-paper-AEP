\* EXPECT: fail P1_VersionMonotone
\* R1-3, CONFIRMED BY PROBE in docs/22 and reproduced here as a model
\* counterexample: P1 is a statement about ONE monotonic Redis timeline.
\* Removing that assumption un-fences a writer that was correctly fenced.
\* docs/26:214 asks for exactly this figure.
\*
\* Everything else holds. The barrier is on and truthful; only the
\* single-timeline assumption is gone. There is no local fix -- the fix is
\* consensus, which is non-claim NC-3.
\*
SPECIFICATION FairSpec

CONSTANTS
    Workers              = {w1, w2}
    IntentIds            = {i1, i2}
    MaxVersion           = 5
    MaxAttempts          = 2
    Capability           = "AUTHORITATIVE_READBACK"
    BarrierEnabled       = TRUE
    TruthfulFsync        = TRUE
    SingleTimeline       = FALSE
    TruthfulEndpoint     = TRUE
    OperatorEnabled      = FALSE
    ReconcileDelayCovers = TRUE

INVARIANTS
    TypeOK
    P2_AtMostOneUnresolved
    P3_BoundedAttempts
    NoUndetectedDuplicate
    NoLostEffect

PROPERTIES
    P1_VersionMonotone
    P1_NoStatusRegression
    P2_LegalEdgesOnly
    P2_NoReentry
    P2_EventuallyResolved
