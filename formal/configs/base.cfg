\* EXPECT: pass
\* Base configuration: every stated assumption holds.
\* This is the configuration in which the paper's properties are claimed.
\* Bounds: 2 workers, 2 intent slots, 5 versions, attempt budget 2
\* (docs/26:212 asks for 2 workers and 3 versions; this is that or more).
SPECIFICATION FairSpec

CONSTANTS
    Workers          = {w1, w2}
    IntentIds        = {i1, i2}
    MaxVersion       = 5
    MaxAttempts      = 2
    Capability       = "AUTHORITATIVE_READBACK"
    BarrierEnabled   = TRUE
    TruthfulFsync    = TRUE
    SingleTimeline   = TRUE
    TruthfulEndpoint = TRUE
    OperatorEnabled  = FALSE
    ReconcileDelayCovers = TRUE

INVARIANTS
    TypeOK
    P2_AtMostOneUnresolved
    P3_BoundedAttempts
    NoUndetectedDuplicate
    NoLostEffect

PROPERTIES
    P1_VersionMonotone
    P1_NoStatusRegression
    P2_LegalEdgesOnly
    P2_NoReentry
    P2_EventuallyResolved
