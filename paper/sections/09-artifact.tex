\section{Artifact Availability}
\label{sec:artifact}

The public repository \artifactavail{} contains the implementation, evaluation
harness, mock provider, baseline systems, analysis pipeline, manuscript source,
and the tracked derived analysis products used by the number generator. It does
\emph{not} contain the raw run directories, which are bulk data: those are
carried by a separate archive, \archiveavail{}.

\paragraph{The raw evidence archive} Two archives under one DOI: the
2026-09-03 build (20 collection roots, 1\,458 run directories, 26\,300
files) and a 2026-09-15 extension covering the trees it did not reach (11
roots, 1\,332 run directories, 18\,494 files) --- 2\,790 run directories and
44\,794 files in all, 848\,MB uncompressed and 42\,MB compressed. Between
them they carry the 432-run evaluation, the \texttt{appendfsync=always} arm,
the deployment sweep of \cref{sec:eval-rq3}, the real-Temporal baseline, the
write-loss cell, the voided oracle-disagreement run, every prevention
replication, and the arms voided for having run against the wrong container
runtime. Every file carries a SHA-256 in its own archive's
checksum manifest, and that manifest's own digest ---
\texttt{87fa2d53\ldots} and \texttt{54d1ab0f\ldots} --- attests that archive;
the two share no path and each is verified only against its own. Both were
extracted and checked file-by-file before deposit, 26\,300 and 18\,494
entries, none failing or missing. The 2026-09-03 archive was additionally
re-analysed --- 114 tracked analysis products byte-identical, 8 identical
after two declared normalisations recorded with the deposit, none differing
--- and \emph{that re-analysis has not been repeated for the extension}.
% Numbers discipline (main.tex header). These are properties of the deposit,
% not of a tracked CSV, so they are not macros: their sources
% (ARCHIVE-METADATA.json and MANIFEST.sha256 inside each archive) live
% outside the repository, and a generated macro reading them would fail in
% the clean clone an evaluator uses. Each figure below names its source.
%   20 roots / 1 458 run dirs / 26 300 files / 87fa2d53... / 3aa90b21... :
%     aep-raw-archive/{ARCHIVE-METADATA.json,MANIFEST.sha256},
%     docs/29 1, ARTIFACT.md 5, reports/phase-report-11-rescue-2026-09-03.md
%   11 roots / 1 332 run dirs / 18 494 files / 54d1ab0f... :
%     aep-raw-archive-ext/{ARCHIVE-METADATA.json,MANIFEST.sha256},
%     docs/29 0b, ARTIFACT.md 5,
%     reports/phase-report-35-archive-extend-2026-09-15.md
%   848 MB = 492 905 568 + 355 492 262 payload bytes; 42 MB = 24 257 505 +
%     17 563 367 compressed. 2 790 = 1 458 + 1 332; 44 794 = 26 300 + 18 494.
%   A run directory is one holding a single run's artifacts. The extension's
%     ARCHIVE-METADATA.json reports 992, not 1 332, because its counter
%     counts run-config.json files and 340 directories in four roots -- the
%     three B5/Temporal session attempts and one voided write-loss arm --
%     carry run artifacts without one, B5 running through session.py rather
%     than run_matrix. docs/29 3 discloses this to a reader who opens the
%     metadata. Counted by scripts/verify_published_archive.py.
%   114 / 8 / 0: scripts/verify_raw_archive.py over the 2026-09-03 archive
%     only, most recently in the phase-39 clean clone. The extension has no
%     equivalent run, which is why the sentence says so.

\paragraph{What is pinned} The Python environment is locked
by a resolved dependency lockfile; the Redis image is pinned by digest in the
compose file;
the matrix records a seed per run and emits its full plan, cost estimate and
platform fingerprint \emph{before} launching. Continuous integration
provisions Redis from the same compose file the experiments use, runs the full
suite, the \texttt{WAITAOF} durability suite, a citation-range check over
the formal model and the numbers gate described below, and fails if any test is
skipped.

\paragraph{What can be reproduced, and at what cost} The matrix is resumable
and its cells are independent. The full plan is 1\,068 runs at an estimated
25\,h of wall time on one machine; the cells behind the results in
\cref{sec:evaluation} are a subset keyed in the tracked per-cell manifest. The raw archive carries the complete per-cell and
per-run manifest alongside the runs, so a reader can see what was collected
rather than inferring it from the tables.

\paragraph{Regenerating the paper's numbers} The tables and every headline
scalar in \cref{sec:evaluation} are generated by
the artifact's table generator from the tracked inputs its documentation
enumerates: per-cell and per-execution metrics, latency under both
fsync policies, the regime-labelled comparisons, the Redis-kill ablation,
coverage, and the write-loss probes. Each emitted value carries a comment
naming the filter and arithmetic behind it.
The artifact's build script builds the manuscript and runs that
check; it treats an undefined reference and a BibTeX parse error as build
failures rather than warnings, because both have produced a clean-looking
build of this paper once each.
An independent number check re-derives the tables and fails if
the manuscript has drifted from the results. It also fails if a generated
number is defined and never used, which is the drift a framing revision
produces:
LaTeX catches a macro used without a definition and is silent about a claim
whose evidence was orphaned when the claim moved. The pooled summary table
produced by the analysis tool is deliberately \emph{not} a source for any
number here. All comparisons used by the paper are filtered to an explicit
regime; the headline and B3 comparisons use only the crashed regime.

\paragraph{The host-level write-loss probe} \Cref{sec:eval-durability} needs a
block device that stops accepting writes, which needs root, a loop device and
a \texttt{dm-flakey} target, so the write-loss probe is
not part of the test suite --- a test that skips wherever those are
unavailable is the failure mode this project's CI gate exists to prevent. What
\emph{is} in the suite is everything about the probe that can be checked
without a kernel: that its ``drop'' table really carries \texttt{drop\_writes},
that its pass-through table carries no feature at all, and that a trial whose
acknowledged write vanished is voided rather than counted. The probe itself
refuses to report any Redis trial until a self-test has shown, on the bare
device, that a file \texttt{fsync}-ed before the switch survives and one
\texttt{fsync}-ed after it does not.

\paragraph{The state-machine figure} \Cref{fig:statemachine} is generated by a script
that imports the transition set from the implementation. An edge added to or removed from the protocol changes the
figure or fails the check; it cannot silently disagree with the code.

\paragraph{Citations} Every bibliography entry was verified to exist before it
was written: matched against DBLP's search API, or resolved through
\texttt{doi.org}, or --- for vendor documentation --- fetched and read on a
recorded date. The verification script and bibliography metadata are tracked.
The raw lookup transcript is not present in the Git repository and is not in
the raw-evidence archive either, which carries run data only; it is the one
item named in this section for which no external copy yet exists.
