% Supplementary material for
%   "Declared Ambiguity: Fail-Closed Execution for Non-Idempotent Legacy APIs
%    Without Idempotency Keys"
%
% WHY THIS IS A SEPARATE DOCUMENT AND NOT AN APPENDIX TO main.tex.
%
% The IEEE Computer Society author guidance, which governs TSE, states that
% "appendices in journal articles are treated as supplemental material and must
% be submitted separately rather than in the main manuscript file", and that
% "Supplemental material is not included in these word counts". Both sentences
% point the same way: an appendix bound into main.tex would still be part of the
% manuscript file, would still count against the 12-page regular-paper limit,
% and would have to be pulled back out at submission. A separate PDF is what the
% submission process asks for, so that is what this is. Convenience did not
% decide it -- if anything an appendix would have been easier to build.
%
% CROSS-REFERENCES. Being a separate document, this one cannot \cref labels
% defined in main.tex. References to the paper are therefore by name, spelled
% out, so a reader holding only this file can still find the passage. That is a
% deliberate cost of the separation and not an oversight.
%
% It carries the same anonymity toggle as main.tex, and build_paper.sh applies
% the same pdfTeX primitives to its anonymous build, because a second PDF that
% leaks the author is the same failure as the first one leaking.

\documentclass[10pt,journal,compsoc]{IEEEtran}

\usepackage[T1]{fontenc}
\usepackage{amsmath}
\usepackage{booktabs}
\usepackage{tabularx}
\usepackage{graphicx}
\usepackage[hidelinks]{hyperref}
\usepackage[capitalise,noabbrev]{cleveref}

% Numbering stops at subsubsection. IEEEtran compsoc numbers \paragraph
% too, which printed run-in headings as "6.3.2.1" and "8.1.0.11" -- a
% depth journal papers do not normally number, and one that reads as a
% defect. 3 and not 2: at 2 the subsubsections go unnumbered as well, and
% the main paper's subsubsection labels then
% all resolve to "6.3" -- sixteen references rendering one number for
% three different targets. Measured, not assumed: both depths were built
% and their .aux compared.
\setcounter{secnumdepth}{3}

% The same generated macros the paper uses. Numbers here are generated by
% scripts/paper_tables.py exactly as the paper's are; none is typed by hand,
% and check_paper_numbers.py scans this file so a macro used only here is not
% reported orphaned.
\input{generated/numbers}

% ---------------------------------------------------------------------------
% Anonymity toggle. Identical in shape to main.tex's: the flag is defined on
% the pdflatex command line by build_paper.sh --anonymous, so neither branch can
% be reached by editing this file alone, and an identity added to the wrong
% branch cannot reach supplementary-anon.pdf.
% ---------------------------------------------------------------------------
\newif\ifanonymous
\anonymousfalse
\ifdefined\ANONYMOUS \anonymoustrue \fi

\begin{document}

\title{Supplementary Material for\\
\emph{Declared Ambiguity: Fail-Closed Execution for Non-Idempotent Legacy
APIs Without Idempotency Keys}}

\ifanonymous
  % Mirrors main.tex's anonymous branch, including the DocInfo wipe. A second
  % PDF that leaks the author is the same failure as the first one leaking.
  \author{Anonymous Author(s)}
  \hypersetup{pdfauthor={},pdftitle={},pdfsubject={},pdfkeywords={},
              pdfcreator={},pdfproducer={}}
\else
  % Deliberately empty until the main paper's byline is settled, so the two
  % documents cannot disagree. What check_paper_numbers.py asserts is the leak
  % that actually matters: that the anonymous supplementary carries the
  % anonymous byline and does NOT contain the main paper's public byline.
  \author{}
\fi

\maketitle

\section{Purpose and status}

This document holds material referenced from the main paper but not required to
follow its argument: the probe detail behind one negative result, the
per-session tables of the uncontrolled hard-Redis-kill cell, the enumerated
coverage gaps of the fault-injection matrix, and the measurement incidents found
during collection. Sections appear in the order the main paper reaches them. It
is submitted as supplemental material rather than as an appendix, for the reason
recorded at the top of this file.

Every number here is generated by the same script that generates the paper's,
from the same frozen results, and is checked by the same gate. Section names in
the main paper are given in words rather than as cross-references, because the
two documents are compiled separately.

\section{Figures and tables referenced from the main paper}
\label{supp:floats}

Three floats live here rather than in the manuscript, each with a pointer left
in the section that used it. They are reproduced without change; only their
location moved.

\subsection{The trade, per system}

Referenced from the main paper's RQ1 subsection, which states the result in
prose and points here for the picture.

\begin{figure}[h]
\centering
\includegraphics[width=\columnwidth]{figures/figure-1-undetected-vs-ambiguity.pdf}
\caption{The trade, per system. Crashed regime only, pooled over crash points
and endpoint capabilities weighted by executions. The three systems on the
right have an empty left bar; only AEP-full and its ablation B3 have a right
bar at all. Interval is Wilson on the pooled counts --- the cluster-aware
bootstrap intervals, which are wider, are in the
artifact's per-cell metrics and are the ones to quote.}
% Generated by experiments/analyze.py write_figures(), from per-cell-metrics
% filtered to regime "(session-3)". NOT from table-1.csv, which pools regimes.
\label{supp:fig:trade}
\end{figure}

\subsection{Declared ambiguity by crash point}

Referenced from the main paper's RQ1 subsection, which keeps the two cells that
carry the argument --- \texttt{before\_intent\_write} at zero and
\texttt{after\_barrier\_before\_dispatch} at its maximum --- and points here for
the full decomposition across all six crash points and three capability classes.

\input{generated/table-ambiguity-by-crashpoint}

\subsection{Selected design assumptions, by the three questions}

Referenced from the main paper's related-work section, whose three subsections
are this table's rows in prose.

% Reproduced verbatim from the manuscript's related-work section. Kept inline
% rather than \input from a new directory, so nothing about the build staging
% has to change to carry it.
\begin{table*}[h]
\centering
\caption{Selected design assumptions, organised by the three questions of the
main paper's related-work section. ``Endpoint must cooperate'' denotes
idempotence, an idempotency key, transaction enrolment, or compensation. The
rows are not ordered by guarantee strength; they expose different residuals when
their stated assumptions fail.}
\label{supp:tab:related}
\small
\renewcommand{\arraystretch}{1.08}
\begin{tabularx}{\textwidth}{@{}>{\raggedright\arraybackslash}X>{\raggedright\arraybackslash}p{0.19\textwidth}c>{\raggedright\arraybackslash}p{0.18\textwidth}@{}}
\toprule
 & endpoint must & can report & residual on \\
 & cooperate & \emph{unknown} & failure \\
\midrule
Lease / fenced CAS~\cite{gray1989leases,kleppmann2016locking}
  & n/a & no & duplicate effect \\
Two-phase commit~\cite{skeen1981nonblocking,mohan1986rstar}
  & prepare and vote & no & blocked participant \\
Sagas~\cite{garciamolina1987sagas,korth1990compensating}
  & compensation & no & compensation gap \\
Transactional outbox~\cite{richardson-transactional-outbox}
  & idempotent consumer & no & duplicate delivery \\
\texttt{Idempotency-Key} header~\cite{jena2025idempotencykey}
  & key support & no & duplicate effect \\
Locks with intent~\cite{setty2016olive}
  & effect inside storage & no & --- \\
Dataflow snapshots~\cite{chandy1985snapshots,carbone2015abs}
  & transactional sink & no & duplicate external effect \\
Beldi~\cite{zhang2020beldi}
  & yes & no & --- \\
Durable Functions~\cite{burckhardt2021durable}
  & yes & no & duplicate or loss \\
Boki~\cite{jia2021boki}
  & yes & no & --- \\
ExoFlow~\cite{zhuang2023exoflow}
  & yes & no & --- \\
Temporal-style, $\infty$ retries~\cite{temporal-retry}
  & yes & no & duplicate effect \\
Temporal-style, 1 attempt~\cite{temporal-retry}
  & yes & no & lost effect \\
\midrule
\textbf{AEP} & \textbf{no} & \textbf{yes} & \textbf{declared ambiguity} \\
\bottomrule
\end{tabularx}
\end{table*}

\section{Why the provably-empty cell is not resolved, and what closing it would
cost}
\label{supp:provable}

The paper's RQ1 subsection reports the negative result --- the
\texttt{after\_barrier\_before\_dispatch} cell is provably empty in the world
and not resolvable from the store, so recovery fails closed --- and points here
for the probe detail and the price of closing it.

\paragraph{Why the store cannot tell the two histories apart} The last durable
write before the external call is the dispatch authorization. The runner then
performs preflight, mints the capability and calls the connector \emph{without
touching Redis again}. A recovery process reading the store afterwards sees an
authorized, unresolved intent --- and that is exactly what it would see had the
worker died \emph{during} transmission, with the request already on the wire.
The two histories are identical in everything durable, so no amount of reasoning
over the store separates them, and failing closed is the correct behaviour given
what recovery can observe rather than a conservatism that better engineering
would remove.

\paragraph{What closing it would cost} A third durable barrier immediately
before transmission would split the two cases and let recovery refute the
earlier one. The measured price of a barrier on this configuration is
\BarrierCostEach{}\,ms, so that is a \ThirdBarrierStepPct{}\% increase in step
latency to convert one crash point's declared ambiguities into confirmed
non-events. It also \emph{narrows rather than closes} the window: a crash
between that barrier and the socket write returns the same ambiguity, so the
trade buys a smaller residual and not the absence of one. We record the trade
rather than take it. Its value depends entirely on a deployment's ratio of crash
points, which our uniform matrix deliberately does not model, so we have no
basis on which to recommend it either way.

\section{The uncontrolled hard-Redis-kill cell, per session}
\label{supp:uncontrolled}

The paper's prevention subsection keeps the replication claim and its direction,
and points here for the per-session tables. Nothing in this section is load
bearing for the paper's argument: the controlled fault carries the result, and
this cell is retained as the replication that agrees in direction at a magnitude
the uncontrolled injector sets.

\begin{table}[h]
\centering
\caption{The \emph{uncontrolled} hard-Redis-kill ablation --- the original
\texttt{docker kill} cell. Identical faults; the only difference is whether the
system waits for the durability acknowledgement before dispatching. The rate is
over executions, one per run. Source: the artifact's Redis-kill ablation
metrics, regime \texttt{redis-kill-preack}, \textsc{no-readback}.}
\label{supptab:killablation}
\small
\begin{tabular}{@{}lcccc@{}}
\toprule
 & runs & \multicolumn{2}{c}{unwanted applied effect} & canary \\
 & & count & rate & survived \\
\midrule
AEP-full & \AepKillRuns{} & \AepKillApplied{} & \AepUnwantedRate{} & \AepKillCanary{} \\
B3 (no barrier) & \BthreeKillRuns{} & \BthreeKillApplied{} & \BthreeUnwantedRate{} & \BthreeKillCanary{} \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{The magnitude moves across sessions} The cell was re-collected
\ReplicationSessions{} further times under an identical configuration --- same
cell identity hash, same per-run seed, same pinned Redis image --- for
\ReplicationRuns{} executions per arm. AEP-full's unwanted-applied count ranged
\ReplicationAepMin{}--\ReplicationAepMax{} out of \AepKillRuns{}; B3 recorded
\ReplicationBthreeApplied{}/\AepKillRuns{} in every one, a range of
\ReplicationBthreeRange{}. Taking the session rather than the execution as the
unit, the difference averaged \ReplicationPreventedMean{} effects, 95\% interval
[\ReplicationPreventedLow{},\,\ReplicationPreventedHigh{}], and the
\UnwantedPrevented{} of the table above falls inside it.

\paragraph{The variation has a candidate cause that this design cannot
establish} Taking the session as the unit, the four sessions separate the runs
that applied an effect from those that did not by \KillLatencyPerSession{}\,ms:
mean \KillLatencyMean{}\,ms, 95\% interval
[\KillLatencyLow{},\,\KillLatencyHigh{}]. \textbf{The half-width is
\KillLatencyHalfWidth{}\,ms, \KillLatencyPrecisionRatio{} times the mean it
brackets}, and the interval contains zero. We give the four session differences
individually because the summary is what fails here: the interval admits effects
three times the mean and effects of the opposite sign, and this design does not
distinguish them. We give no $p$, and we do not put a count of sessions on one
side of zero in place of one. Over \KillLatencySessions{} sessions a two-sided
sign test cannot return below \KillLatencySignFloor{}, so no outcome this design
admits could have reached significance.

\paragraph{B3 is not the negative control this paper previously reported}
Pooled over the same \ReplicationRuns{} executions it read as a clean null, and
pooling is what made it read that way. At the session level it is
uninformative: \KillLatencyBthreePerSession{}\,ms, mean
\KillLatencyBthreeMean{}\,ms, interval
[\KillLatencyBthreeLow{},\,\KillLatencyBthreeHigh{}], a half-width of
\KillLatencyBthreeHalfWidth{}\,ms and \KillLatencyBthreePrecisionRatio{} times
its own mean. A comparison that wide could not have contradicted the mechanism
whatever B3 had done, so its failure to contradict it was never evidence for it.

The original cell was collected on a different filesystem from the four
replications, a difference no run configuration records, so we report it
separately: it agrees in direction at \KillLatencyOrigDiff{}\,ms over
\KillLatencyOrigN{} runs ($p = \KillLatencyOrigP{}$), with its own control at
\KillLatencyBthreeOrigDiff{}\,ms ($p = \KillLatencyBthreeOrigP{}$,
$n = \KillLatencyBthreeOrigN{}$). \textbf{It is one session: it pools nothing,
and it replicates nothing.}

\section{Coverage of the fault-injection matrix}
\label{supp:coverage}

The main paper's external-validity subsection states the headline coverage and
points here. The full plan is 1\,068 runs; we collected \RunsCollected{},
comprising \ExecutionsCollected{} executions over \CellsCollected{} cells,
chosen to answer the research questions rather than to fill the grid. The run
count for every cell is in the matrix manifest and the coverage record that
ship with the artifact.

What that leaves uncovered, precisely --- six named gaps, of which five have
since been closed and are recorded here with what closing them changed:

\begin{itemize}
\item \textbf{B4's and B4b's \textsc{auth} cells were incomplete and are now
      collected --- and completing them corrected the value.} In an earlier
      draft B4's \textsc{auth} cell held twenty executions and B4b had none, so
      that cell printed as a dash. Both are now $n=\BfourExecAuth{}$ and
      $n=\BfourbExecAuth{}$ over all six crash points, the same shape as every
      other cell in the paper's outcomes table, and that table is free of
      dashes. We record what the completion changed, because it is a caution
      about partial cells generally and not only about this one: the earlier
      twenty executions came from a \emph{single} crash point, so the figure
      that draft quoted was not a wide interval around the value the full cell
      reports --- it was one crash point standing in for six, and the six differ
      enough that none of them represents the pooled rate. The full cell puts
      B4's \textsc{auth} duplicate rate at \BfourDupAuth{}, in line with its
      other two classes and far from the figure the partial cell suggested. The
      direction of the argument is unchanged, which is why we could describe the
      gap as non-essential before collecting it, but the number we quoted for it
      was wrong, and a partial cell being unrepresentative rather than merely
      imprecise is the more general lesson. Every other cell in the outcomes
      table spans all the crash points that apply to its system: six for the
      systems that run \texttt{aep\_core}'s workflow, and five for B0--B2, which
      have no window between writing a record and acknowledging it durable
      because they write no record.

\item \textbf{The prevention result no longer rests on one cell; it still rests
      on one host.} The original hard-Redis-kill ablation was collected on
      \textsc{no-readback} only, at one crash point, at \AepKillRuns{} runs per
      system, and was the whole of the barrier's measured case. The paper's
      prevention subsection now leads with a controlled-fault collection over
      \ArmASessions{} sessions and all \ArmAClasses{} capability classes, whose
      between-session spread is \ArmASpreadMax{} count per class. What has not
      changed is the machine: every collection here comes from one host, so the
      effect size's dependence on host timing is now measured \emph{on} that
      host rather than established \emph{across} hosts, and a second machine
      would still be sampling something we have not observed.

      We predicted that the other two capability classes would move the
      \emph{declared-ambiguity} column and not the applied-effect column,
      because whether an effect reached the provider is not something a
      read-back capability can change after the fact. We since collected those
      cells: \ClassSessions{} pre-registered sessions, \ClassRunsPerArm{} runs
      per arm per session, run-level interleaved, with the analysis fixed in
      advance. \textbf{The applied-effect column moved, and it moved in both
      directions.} The \textsc{authoritative-readback} minus
      \textsc{no-readback} applied-rate difference ran \ClassPpOne{},
      \ClassPpTwo{}, \ClassPpThree{} and \ClassPpFour{} percentage points across
      the \ClassSessions{} sessions --- a spread from \ClassPpMin{} to
      \ClassPpMax{} points on a comparison we expected to sit at zero. That is
      our own prediction contradicted in our own data, and we report it rather
      than resolving it.

      The session-clustered interval on that difference contains zero, at
      [\ClassPpLow{},\,\ClassPpHigh{}] percentage points. \textbf{It contains
      zero because the sessions disagree with one another, not because the
      effect is absent:} the half-width is \ClassPpHalfWidth{} points, wider
      than the mean it brackets at \ClassPpMean{} points, so this design could
      not have resolved the effect it observed. We registered a minimum
      detectable effect in advance and do not quote it here, because it was
      derived without a between-session variance component and so understates
      the precision this question needed; the comparison we make instead is the
      half-width against the observed mean. \textbf{What we report is therefore
      a failure to reject at a precision inadequate to the question --- not
      evidence that capability class leaves the applied-effect column alone.}
      The argument above is no longer unsupported: it is contradicted in the
      data, and untested at adequate power.

\item \textbf{The in-flight Redis-kill variant} (kill during transmission rather
      than before acknowledgement) \textbf{is now collected, and it ties.} Over
      \InflightSessions{} sessions the two arms differ by at most
      \InflightMaxDiff{} count in any capability class, both at ceiling: the tie
      our analysis predicted, measured rather than inferred. Two qualifications
      travel with it. The second session is a \emph{deterministic replay} of the
      first rather than an independent draw --- the harness assigns seeds per
      cell and repetition, so where no race remains the second collection
      reproduces the first exactly, which is itself the clearest evidence that
      none does. And B3's lowest cell is \InflightBthreeFloor{}/\AepKillRuns{},
      exactly the floor below which we would have read the result as a mis-timed
      injector rather than as a tie; it is the boundary, not headroom.

\item \textbf{The $30\%$ crash-probability regime is now collected, and the
      paper's tables still do not report it.} It was implemented but
      uncollected in an earlier draft. The deployment sweep collected it at
      \texttt{mid\_dispatch} for every system in the matrix, under real
      \texttt{SIGKILL} --- \PthirtyRuns{} runs over \PthirtyExecutions{}
      executions --- and the collection is in the artifact. What has not
      changed is the reporting: every rate quoted in this paper still comes
      from either an all-crash or a no-crash regime, so the intermediate
      regime is evidence a reader can inspect rather than a column in a
      table here.

      \textbf{The prediction registered for it was refuted, and is recorded
      as refuted rather than reinterpreted.} Of \PthirtyComparisons{}
      system-by-metric comparisons, \PthirtyBetween{} lie between the
      crash-free and all-crash rates and \PthirtyExceptions{} do not. Both
      exceptions are lost-effect rates: B0 at \PthirtyBzeroPzero{} crash-free,
      \PthirtyBzeroPthirty{} at $30\%$ and \PthirtyBzeroCrashed{}
      all-crash; B4 at \PthirtyBfourPzero{}, \PthirtyBfourPthirty{} and
      \PthirtyBfourCrashed{}. Both violations sit in the frozen crash-free
      arm, where that rate is \PthirtyPzeroEvents{} event in
      \PthirtyPzeroExecs{} executions --- a single observation, not a regime
      interaction.
      % The tally and both exception rows are generated:
      % scripts/paper_tables.py pools each regime over crash points and
      % capability classes, within a regime and never across them, from
      % ws5-2026-09-10/t2-p30/analysis/per-cell-metrics.csv (p30) and
      % analysis/per-cell-metrics.csv (p0 and crashed). It refuses to emit
      % if the exception set stops being exactly B0 and B4 on
      % lost_effect_rate, so the two systems named above cannot go stale
      % silently. Reproduces the table in reports/phase-report-17-ws5-
      % analysis-2026-09-14.md section H3 exactly.
      % experiments/results/ws5-2026-09-10/t2-p30/analysis/coverage.json:
      % regimes [p30], crash_points [mid_dispatch], seven systems,
      % all_runs_used_real_sigkill true. Hypothesis H3 and its two
      % one-event violations: reports/phase-report-17-ws5-analysis-
      % 2026-09-14.md, section H3. No rate from this collection is quoted
      % anywhere in the manuscript, so no macro exists for one.

\item \textbf{The alternative read-back keying is now collected, and it
      refuted its own prediction.} It was pre-registered as a sensitivity
      check predicted \emph{null} within a $\pm\BthreeVsAepAmbMargin{}$
      percentage-point margin. It is not null. Keyed by an oracle
      fingerprint of the request, declared ambiguity on \textsc{pos-only} is
      \KeyingAmbiguityOracle{}\% against \KeyingAmbiguityCaller{}\% under the
      caller's own reference --- \KeyingAmbiguityDelta{} percentage points,
      nearly twice the margin. The two columns that carry the safety claim,
      undetected duplicates and lost effects, are zero under both keyings, so
      what moves is the residual and not the guarantee. The main paper's RQ1
      subsection states this; it is recorded here as a closed gap because the
      prediction that closed it was wrong.
      % generated/numbers.tex | ws5-2026-09-10/t2-keying/analysis/
      % per-cell-metrics.csv (keying=ORACLE_FINGERPRINT, 399/900) against
      % analysis/per-cell-metrics.csv (keying=CALLER_REFERENCE, 63/180);
      % metric=known_ambiguity_rate, system=AEP_FULL,
      % response_class=POSITIVE_ONLY_READBACK, pooled over the six crash
      % points. Pre-registered as H4 in reports/phase-report-ws5-prediction-
      % 2026-09-10.md section 2.2; refuted in reports/phase-report-17-ws5-
      % analysis-2026-09-14.md section H4.

\item \textbf{One run did not complete} and is listed as such in the manifest
      rather than dropped silently.
\end{itemize}

A rate we did not measure is absent from the paper's tables rather than
interpolated, and the manifest shipped with the results states the run count for
every cell so a reader can see the shape of the coverage without reconstructing
it.

\section{Measurement incidents found during collection}
\label{supp:incidents}

The main paper's internal-validity subsection states each of these in one
sentence and points here. All three are defects in the \emph{measurement}, found
by the fault-injection matrix rather than by the unit suite, and all three are
fixed; they are recorded because the general point --- that a fault-injection
harness is a stronger verification tool than a unit suite --- cuts both ways,
and there may be defects it has not found.

\subsection{A resume defect that would have inflated counts}

Our matrix runner supports resumption, and a run whose previous attempt was
interrupted left event shards on disk that a fresh attempt could merge into its
own log, double-counting executions that were never part of the run being
recorded. It surfaced as a configuration-digest mismatch --- the digest check
did what it exists to do --- but between two attempts of the same harness
version the merge would have been \emph{silent}, and the error class is ``a
resumed run over-counts'', which for a paper measuring rates is as bad as errors
get. The safeguard is now explicit: the runner discards stale event shards and
any stale summary before a run writes anything, logs what it discarded, and
leaves the run's \emph{inputs} --- the rendered provider config and the oracle
ledger --- untouched, with a test pinning that distinction. Every resumed run in
the current results was collected after the fix; runs collected before it were
re-analysed and the single affected run was re-collected.

\subsection{Two further defects the matrix found that the unit suite had not}

A re-executing supervisor abandoned the lease instead of waiting for it, and
execution-state seeding was not idempotent. Both made a \emph{baseline} look
better than it is, so both were biasing against our own hypothesis, and both are
fixed.

\subsection{The test suite is destructive to a running matrix}

While collecting the B4/B4b cells we ran the project's own integration suite
against the same Redis. The suite is deliberately destructive: it deletes the
\texttt{aep:*} namespace in the database the matrix is using, and one of its
durability tests \texttt{docker restart}s the matrix's Redis container by name to
prove the AOF survives. Several runs died with connection errors, and the server
the batch was collecting against was replaced underneath it.

Runs that \emph{fail} are harmless --- they write no summary, and the resumption
logic re-runs them. The runs that \emph{completed} during that window are the
hazard, because a Redis blip makes a re-executing baseline retry, a baseline that
retries more produces more duplicates, and that bias points toward our own
hypothesis. They are also indistinguishable from clean runs in every artifact the
analysis reads. Every B4/B4b run collected in the disturbed window was therefore
discarded and re-collected on an otherwise idle host, and the server's
\texttt{run\_id} was recorded before and after the replacement batch to confirm
it was not restarted again; none of the discarded runs contributes a number to
the paper.

Two rules follow, and the artifact enforces both rather than documenting them:
the runner refuses to start beside a host-level fault injector, and it now
records the server's \texttt{run\_id} with every run so that a restart during
collection is visible in the results rather than inferred from a connection error
that happened to be logged. We report the episode because the generalisation is
not ``be careful'': it is that a fault-injection harness and a fault-injection
test suite make the same assumptions about owning the system, and running both is
a measurement error that produces ordinary-looking data.


% ---------------------------------------------------------------------
% Moved out of the evaluation section in WS-9's length pass. It is a
% decision aid rather than a claim: section VI-RQ3 states both barrier
% costs with their intervals and does not depend on this table. Every
% number and provenance comment below is unchanged from the manuscript.
% ---------------------------------------------------------------------
\section{The barrier is a deployment choice, with three measured points}
\label{supp:deployment}

The $\approx 2$-second figure above is the one a reader would carry away as
``AEP's overhead''. It is not: it is the price of one of the protocol's two
guarantees, under one of the durability settings that guarantee can be bought
at. \Cref{tab:deployment} is the decision as a deployment actually faces it.

\input{generated/table-deployment-choice}

Three points, and the third is the one the earlier framing of this paper
concealed.

\paragraph{\texttt{everysec} with the barrier} The default, and the expensive
one. An acknowledgement must wait for the next scheduled fsync, up to a second
away, and the protocol waits twice.

\paragraph{\texttt{always} with the barrier} The identical crash-free cells ---
same systems, endpoint, regime, seeds, host and provider --- against a Redis
whose only changed line is \texttt{appendfsync always}.
% source: reports/raw/f0iii-appendfsync-always.txt and the G2 re-run that
% added the B3 arm; both refuse to proceed unless CONFIG GET appendfsync
% reads back 'always' on the live server.
The barrier's cost is \BarrierCostAlwaysFortyFive{}\,ms
[\BarrierCostAlwaysFortyFiveLow{}, \BarrierCostAlwaysFortyFiveHigh{}]\,ms over
fifteen runs per arm: negative, and not distinguishable from zero. Under
\texttt{always} the fsync has already happened by the time the write returns,
so \texttt{WAITAOF} has nothing left to wait for.
% generated/numbers.tex | fsync-always-2026-09-14/analysis/
% latency-and-throughput.csv and per-execution.csv, same bootstrap as above.

\textbf{That is the measurement this section turns on.} The barrier does not
become cheap under \texttt{always}; it costs nothing, and the
\BarrierCostFifteen{}\,ms it costs under \texttt{everysec} is therefore the
price of the fsync boundary rather than of the barrier. A protocol step under
\texttt{everysec} waits for the next scheduled flush, and there are two of
them. Nothing in the mechanism is expensive.

The sign is not a puzzle to explain away. The two medians it separates are
\AepAlwaysFortyFiveMedian{}\,ms with the barrier and
\BthreeAlwaysFortyFiveMedian{}\,ms without it, on a two-second call; the gap
between them is noise around a mechanism that is doing no waiting. This arm was
pre-registered with the interval's exclusion of zero explicitly ruled out as an
acceptance criterion, because a mechanism with nothing to wait for should cost
nothing and a measurement of nothing lands on either side of it.
% generated/numbers.tex | fsync-always-2026-09-14/analysis/
% latency-and-throughput.csv, step_latency_ms_median per system.

Both figures are ablation differences \emph{within} one policy, which required
collecting the barrier-less arm twice. It would have been easier to subtract
the \texttt{everysec} B3 median from the \texttt{always} AEP-full median, and
that would have assumed the ablated protocol's own writes cost the same under
both policies. They nearly do --- B3 is \BthreeStepMedian{}\,ms under
\texttt{everysec} and \BthreeAlwaysFortyFiveMedian{}\,ms under
\texttt{always} --- but
``nearly'' is a measurement, and an order-of-magnitude claim resting on an
unmeasured premise is the kind of number this paper is trying not to publish.

\paragraph{How precise these two figures are} Each is a difference of two
medians over fifteen runs per arm, bootstrapped over runs rather than
executions. Under \texttt{everysec} the barrier costs
\BarrierCostFifteen{}\,ms, [\BarrierCostFifteenLow{}, \BarrierCostFifteenHigh{}];
under \texttt{always} it is \BarrierCostAlwaysFortyFive{}\,ms,
[\BarrierCostAlwaysFortyFiveLow{}, \BarrierCostAlwaysFortyFiveHigh{}]. The first
excludes zero by a wide margin and the second contains it, which is the whole
of the claim: the policy decides whether the barrier is expensive, and the
barrier itself is not.

\paragraph{B3-mode: the barrier removed entirely} Not a baseline we are beating
but a supported configuration of the same implementation, licensed by the detection result in the paper's evaluation section. What it pays over a system with no protocol at all
is not resolvable from zero at fifteen runs per arm
(\ProtocolMinusBarrierFifteen{}\,ms,
[\ProtocolMinusBarrierFifteenLow{}, \ProtocolMinusBarrierFifteenHigh{}]). On the detection metrics, pooled across capability
classes, we observe no difference between it and AEP-full, supported by a bound
rather than a test: both the undetected-duplicate and lost-effect rates lie in
$[0,\AblationZeroUpperRun{}\%]$ at joint coverage of at least 90\%, and the
declared-ambiguity difference lies inside the $\pm\BthreeVsAepAmbMargin{}$
percentage-point margin the paper's detection subsection states as a post hoc
stipulation. \emph{We do not make that claim per capability class, and the
reason is a number rather than an oversight}: six simultaneous bounds instead of
two, over a third as many run clusters, puts the Bonferroni width at
\AblationZeroUpperPerClass{} percentage points, and a band that wide is not a
detection guarantee. What B3-mode forfeits is the prevention result: under
a hard Redis kill it will put an effect on the wire that AEP-full withholds, at
\BthreeUnwantedRate{} against \AepUnwantedRate{}, and its intent record will not
survive a host-level write loss, as the paper's durability subsection
reports. That second
clause is about the \emph{record}, and it does not license the protocol-level
inference a reader may draw from it: when we ran both arms under exactly that
fault, neither withheld, because the storage discarded the writes while
reporting success and the barrier was handed an acknowledgement that looked
good (the paper's write-loss cell).

\textbf{So the question a deployment answers is not ``can we afford AEP'' but
``do we need prevention, and at which fsync policy''.} If losing Redis is the
fault to worry about --- an instance on the same host as the workers, an
unreplicated instance, a VM that can vanish --- the barrier is the point of the
protocol and \texttt{always} makes it nearly free at this workload's shape. If
Redis is well protected and worker crashes are the concern, B3-mode gives the
paper's headline result for a cost this evaluation cannot distinguish from zero.

\textbf{One measurement this table does not support} is what \texttt{always}
costs under load. The Redis
documentation calls it ``very very slow, very safe''~\cite{redis-persistence}, a
judgement about write throughput that our workload cannot exercise: two workers,
a two-second provider delay and a handful of writes per step leave the system
latency-bound and nowhere near an fsync-rate limit. Our throughput figure is in
fact \emph{higher} under \texttt{always} ($\AepThroughputAlways{}$ versus
$\AepThroughputEverysec{}$ executions/s), which is a statement about this
workload's shape and not about Redis.


% ---------------------------------------------------------------------
% Moved out of the evaluation section in WS-9's length pass. The outcome
% classes it discusses are counted in the paper's outcome table, so no
% claim in section VI depends on this text. Numbers and provenance
% comments are unchanged.
% ---------------------------------------------------------------------
\section{RQ4: recovery}
\label{supp:rq4}

Recovery behaviour is reported through the outcome distribution rather than a
success rate, for a reason worth stating: ``recovery success'' means two
different things either side of whether a recovery service was running. Where
one ran, a crashed execution reaching a terminal classification was
\emph{recovered}. Where none ran --- B0, B1, B2, B4, B4b declare none --- the
same execution reached its classification because a supervisor ran the step
again, which is not recovery but the thing recovery exists to avoid. Our
analysis excludes runs without a recovery service from that metric rather than
scoring them zero or one, because both would be statements about a component
that was not present.

Within that scope: every AEP-full and B3 execution in the crashed regime that
had written an intent reached a terminal classification, and the main paper's
RQ4 subsection gives the counts. The executions crashed before an intent
existed are outside that scope rather than failures within it, because no
record exists for recovery to act on. Across \RunsCollected{} runs exactly
one recorded a reconciliation disagreement --- every other completed run's
event log agreed with its independently written oracle ledger. The harness
performs this reconciliation after each completed run; it checks definitive
applied/non-applied assertions, effect-count bounds, attribution, and duplicate
predictions rather than trusting a line count.
% source: analysis/coverage.json and the per-run reconcile step.

That one is worth stating precisely, because the check exists to be believed.
It was a B4b cell in which the system classified all ten executions
\textsc{confirmed-applied} while the provider's ledger recorded two, eight
disagreements of the form ``the system asserted the effect was applied and the
ledger records none''. Both sibling runs of the same cell recorded ten, the
provider logged no error, and its run-log held three lines against eleven for
each sibling --- so the provider recorded almost nothing while answering the
workers successfully. We could not determine the cause. A re-run of the
identical cell, same seed and same configuration, agreed.

We treat it as a \emph{void} run on the rule the durability probe uses: a
trial whose measurement precondition failed says nothing about the system in
either direction. Counting it would let a broken ground truth set a baseline's
rate; discarding it quietly and keeping the re-run would be re-running until
the number looks right. So the re-run is what the paper's outcome table contains,
the voided attempt must ship in the external raw archive, in a
directory reserved for voided runs, with this explanation beside it; it is not
present in the current Git repository. The count above is one rather than zero.
Recovery latency is not reported as an absolute figure: the E5 gate leaves too
few gated crashed runs to support a distribution, and the paper's threats section
records this as a gap rather than filling it with ungated numbers.


% ---------------------------------------------------------------------
% Moved out of the evaluation section in WS-9's length pass. Section VI
% keeps the result -- the cell is empty in the world and unresolvable from
% the store -- and this carries the reasoning and the third-barrier trade.
% The figure it sat beside stayed in the paper: RQ1 references it, and RQ1
% is one of the four blocks that do not move.
% ---------------------------------------------------------------------
\section{Why the provably-empty cell is not resolved}
\label{supp:provable-detail}

The \texttt{after\_barrier\_before\_dispatch} row invites an obvious question:
if the worker died before sending anything, \emph{the system could know that}.
Why is the outcome ambiguous rather than a confirmed non-event?

Because nothing durable records it. A recovery process reading the store
afterwards sees an authorized, unresolved intent --- exactly what it would see
had the worker died \emph{during} transmission, with the request already on the
wire. The two histories are identical in everything durable, so recovery fails
closed, which is the correct behaviour given what it can observe. \textbf{That
is the negative result, and it is the protocol's, not the implementation's:} the
cell is empty in the world and unresolvable from the store. Closing it is
possible and we did not do it. The supplementary material gives the probe detail
and prices the third barrier that would split the two cases --- a trade that
narrows rather than closes the window, and whose value depends entirely on a
deployment's ratio of crash points, which our uniform matrix deliberately does
not model.

% The moved comparison table carries sixteen citations, so this document needs
% its own reference list: it is compiled separately and cannot reach the
% manuscript's. check_paper_numbers.py has a gate for exactly this -- "a
% supplementary that cites declares a bibliography" -- and build_paper.sh
% derives whether to run bibtex from the presence of the line below, announcing
% the decision rather than skipping silently.
%
% The numbering here is this document's own and does not agree with the
% manuscript's: only entries cited in these pages appear, numbered from one.
% Every key resolves against the same paper/refs.bib.
\bibliographystyle{IEEEtran}
\bibliography{refs}

\end{document}
