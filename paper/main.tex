% =====================================================================
% AEP: Declared Ambiguity for Autonomous Agents Calling Non-Idempotent
%      Legacy APIs
%
% Target venue: IEEE Transactions on Software Engineering (TSE).
% Class: IEEEtran, journal mode, double column.
%
% NUMBERS DISCIPLINE (amendment F3). Every numeric claim in this manuscript
% carries a LaTeX comment naming the CSV cell or raw report section it came
% from. The two permitted sources are:
%
%   experiments/results/matrix/analysis/per-cell-metrics.csv
%       -- the ONLY quotable source for rates. Keyed by
%          (regime, system, crash_point, response_class, readback_keying).
%   experiments/results/matrix/analysis/latency-and-throughput.csv
%       -- timings, and only from runs that passed the E5 gate.
%
% Table-1-pooled (analysis/table-1.csv) is BANNED as a source: it pools three
% fault regimes and three response classes, so every rate in it is a property
% of the collected mix rather than of any system. Session 3B section F2 says
% so, and analyze.py prints the warning itself.
%
% scripts/check_paper_numbers.py re-reads the CSVs and fails if any number
% annotated below has drifted from its source.
% =====================================================================

\documentclass[journal,10pt]{IEEEtran}

\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{amsmath,amssymb}
\usepackage{booktabs}
\usepackage{multirow}
\usepackage{graphicx}
\usepackage{xcolor}
\usepackage{tikz}
\usetikzlibrary{arrows.meta,positioning,shapes.geometric,fit,backgrounds}
\usepackage{listings}
\usepackage{url}
\usepackage[hidelinks]{hyperref}
\usepackage[capitalise,noabbrev]{cleveref}

% \todo markers: amendment F4 permits them only where a cell still running at
% the time of writing could change the value.
\newcommand{\todoitem}[1]{\textcolor{red}{\textbf{[TODO: #1]}}}

% Semantic macros for the three outcome classes, so the trilemma reads the
% same way everywhere it appears.
\newcommand{\undetdup}{\emph{undetected duplicate}}
\newcommand{\lostfx}{\emph{lost effect}}
\newcommand{\declamb}{\emph{declared ambiguity}}

\newtheorem{property}{Property}
\newtheorem{definition}{Definition}

% Headline scalars, generated from the frozen CSVs by scripts/paper_tables.py.
% Every macro in here carries a comment naming the file, the filter and the
% arithmetic that produced it. Do not hand-edit; regenerate.
\input{generated/numbers}

% =====================================================================
% ANONYMITY TOGGLE (Monday audit, fix F2).
%
% The manuscript shipped `Anonymous Author(s)` and a URL naming a personal
% GitHub account at the same time: under double-anonymous review the URL
% defeats the anonymisation, and without it the author block is unfinished.
% Rather than pick one, the source now builds both from identical text:
%
%   public build      bash scripts/build_paper.sh              -> main.pdf
%   anonymous build   bash scripts/build_paper.sh --anonymous  -> main-anon.pdf
%
% The flag defines \ANONYMOUS before this file is read, so neither build has
% any prose of its own and the two cannot drift apart.
\newif\ifanonymous
\anonymousfalse
\ifdefined\ANONYMOUS \anonymoustrue \fi

% The artifact pointer lives here, once. Every place that cites the artifact
% uses one of these two macros, so there is exactly one block to audit for a
% leak. Two forms because the two sites need different grammar: \artifacturl
% is a bare pointer after a colon, \artifactavail completes "... are ...".
% The public build renders exactly as it did before this toggle existed.
% The author identity is gated by the SAME switch, and lives in the SAME
% block, for the same reason: one place to audit for a leak rather than two.
% The anonymous branch names nobody and adds no correspondence footnote, so
% an identity added to the wrong branch cannot reach main-anon.pdf.
% The address is set in \texttt rather than \url so it produces no link
% annotation -- an annotation is a second surface an anonymity check has to
% clear, and the Monday audit found four of them the text check had missed.
\ifanonymous
  \newcommand{\artifacturl}{available via the submission system}
  \newcommand{\artifactavail}{available via the submission system}
  \newcommand{\authorname}{Anonymous Author(s)}
  \newcommand{\authorcontact}{}
  % The identifying string must not survive in the document properties either,
  % which is where an anonymised PDF usually leaks. Set empty explicitly rather
  % than relying on them happening to be unset.
  \hypersetup{pdfauthor={},pdftitle={},pdfsubject={},pdfkeywords={},
              pdfcreator={},pdfproducer={}}
\else
  \newcommand{\artifacturl}{%
    \url{https://github.com/hafizmirhamza276-lab/Research-paper-AEP}}
  \newcommand{\artifactavail}{available at%
    \ \url{https://github.com/hafizmirhamza276-lab/Research-paper-AEP}}
  \newcommand{\authorname}{Hamza Khan}
  \newcommand{\authorcontact}{%
    \thanks{Correspondence: \texttt{hafizmirhamza276@gmail.com}.}}
\fi
% =====================================================================

\begin{document}

\title{Declared Ambiguity: The Agent Execution Protocol (AEP) for\\
Autonomous Agents Calling Non-Idempotent Legacy APIs}

\author{%
  \IEEEauthorblockN{\authorname}%
  \thanks{Manuscript prepared \today. Artifact: \artifacturl.}%
  \authorcontact%
}

\markboth{IEEE Transactions on Software Engineering}%
{Declared Ambiguity for Agents Calling Non-Idempotent Legacy APIs}

\IEEEtitleabstractindextext{%
\begin{abstract}
Autonomous software agents increasingly invoke legacy enterprise APIs that are
non-idempotent, accept no idempotency key, and cannot be asked, after the
fact, whether a mutation was applied. When an agent crashes around such a call,
every available strategy loses something. Retrying risks a second real-world
effect that nobody observes. Not retrying risks an effect that exists and that
no record accounts for. We argue that the choice between these is not a
question of engineering quality but a three-way trade, and that a system's
obligation is to make the third corner reachable: to convert silent failure
into \emph{declared, durable, bounded ambiguity} that an operator can act on.
We present the Agent Execution Protocol (AEP), a fail-closed execution protocol
in which every external side effect is preceded by a durably acknowledged
write-ahead intent, every state write is fenced by lock ownership and an exact
expected-version compare-and-swap in one atomic script, and every unresolvable
outcome halts and escalates rather than guessing. We evaluate AEP against five
baseline designs, one of them in two configurations --- naive retry,
lease-only, CAS-only, an ablation without the durability barrier, and a
durable-execution engine in both its vendor-default and at-most-once settings
--- under real \texttt{SIGKILL} process faults, hard Redis kills and
block-level write loss, across three endpoint reconciliation capabilities.

Our central result separates two claims that the write-ahead pattern is usually
sold as one. \emph{Detection} --- no undetected duplicate and no lost effect,
with a residual of declared ambiguity whose rate is set by what the endpoint
can be asked --- is produced by the pre-dispatch record and a transition table
that prohibits re-entry into dispatch. In the crashed regime, the B3 ablation
and AEP-full each have \BthreeVsAepN{} executions: both record
$\BthreeVsAepDupCount{}$ undetected duplicates and
$\BthreeVsAepLostCount{}$ lost effects. The individual one-sided 95\% Wilson
upper bound for either zero-event rate is \AblationZeroUpper{}\%. Declared
ambiguity is \BthreeAmbCount{}/\BthreeVsAepN{} versus
\AepAmbCount{}/\BthreeVsAepN{} (B3 minus AEP-full,
\BthreeVsAepAmbDiffPP{} percentage points; stratified run-cluster
\BthreeVsAepAmbDiffConfidence{}\% interval
[\BthreeVsAepAmbDiffLow{},\,\BthreeVsAepAmbDiffHigh{}] percentage points).
\emph{Prevention} is what the barrier contributes, and the current evidence is
narrower: in one \textsc{no-readback} capability class, at one pre-acknowledgement
Redis-kill point, on one host, its unwanted-applied-effect rate is
\AepUnwantedRate{} versus \BthreeUnwantedRate{} over \AepKillRuns{} runs per
system ($\AepKillApplied{}/\AepKillRuns{}$ versus
$\BthreeKillApplied{}/\BthreeKillRuns{}$, Fisher $p = \UnwantedP{}$) ---
\UnwantedPrevented{} real non-idempotent effects not committed.

The barrier's durability claim, which a process kill cannot exercise at all
because \texttt{appendfsync everysec} defers the \texttt{fsync(2)} and not the
\texttt{write(2)}, we test directly by making the block device stop accepting
writes: acknowledged records survive \FlakeyAckSurvived{} and unacknowledged
ones are destroyed \FlakeyUnackLost{} ($p = \FlakeyBarrierP{}$), against
$\ProcessKillUnackLost{}$ lost under a process kill on the same probe. Because
detection does not
depend on the barrier, its cost is a deployment choice rather than the
protocol's price, and we give three measured points on that curve.
\end{abstract}

\begin{IEEEkeywords}
Fault tolerance, autonomous agents, idempotence, distributed coordination,
write-ahead logging, fault injection, reliability engineering.
\end{IEEEkeywords}}

\maketitle
\IEEEdisplaynontitleabstractindextext
\IEEEpeerreviewmaketitle

\input{sections/01-introduction}
\input{sections/02-motivating}
\input{sections/03-model}
\input{sections/04-protocol}
\input{sections/05-implementation}
\input{sections/06-evaluation}
\input{sections/07-related}
\input{sections/08-threats}
\input{sections/09-artifact}

\bibliographystyle{IEEEtran}
\bibliography{refs}

\end{document}
