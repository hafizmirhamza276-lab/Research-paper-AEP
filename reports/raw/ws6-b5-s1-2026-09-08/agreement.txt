=== WS-6 B5 agreement: ws6-b5-s1-2026-09-08 ===

frozen comparator: experiments/results/matrix/analysis/per-cell-metrics.csv
runs: 120 total, 2 voided as instrument failures and excluded from every rate
       2  VOID_WORKER_NEVER_READY

--- per cell ---
  H1  after_barrier_before_dispatch    AUTHORITATIVE_READBACK   DISAGREES
        B5 0.4000 [0.4000,0.4000] does NOT overlap frozen 0.9333 [0.9000,1.0000] -- B4 would then be an artefact of our model rather than of event-sourced re-execution, and every B4 claim must be re-scoped
  H1  after_barrier_before_dispatch    NO_READBACK              DISAGREES
        2/30 runs voided as instrument failures and excluded from the rate: {'VOID_WORKER_NEVER_READY': 2}
        B5 0.4107 [0.4000,0.4214] does NOT overlap frozen 0.9667 [0.9000,1.0000] -- B4 would then be an artefact of our model rather than of event-sourced re-execution, and every B4 claim must be re-scoped
  H2  after_barrier_before_dispatch    AUTHORITATIVE_READBACK   DISAGREES
        B5 0.3000 [0.3000,0.3000] does NOT overlap frozen 0.9667 [0.9000,1.0000] -- B4 would then be an artefact of our model rather than of event-sourced re-execution, and every B4 claim must be re-scoped
  H2  after_barrier_before_dispatch    NO_READBACK              DISAGREES
        B5 0.3000 [0.3000,0.3000] does NOT overlap frozen 0.8667 [0.8000,0.9000] -- B4 would then be an artefact of our model rather than of event-sourced re-execution, and every B4 claim must be re-scoped
  H1  after_intent_before_barrier      AUTHORITATIVE_READBACK   NOT_TESTABLE_ABSENT_IN_B5
        B5 has no 'after_intent_before_barrier' position: the worker issues an RPC and the server persists it transactionally, so there is no worker-side window between the write and its acknowledgement (B5_SEMANTICS.md 2.3). B4 and B4b HAVE frozen cells here, so this is a hole in B5 where B4 has numbers -- not a shared absence.
  H1  after_intent_before_barrier      NO_READBACK              NOT_TESTABLE_ABSENT_IN_B5
        B5 has no 'after_intent_before_barrier' position: the worker issues an RPC and the server persists it transactionally, so there is no worker-side window between the write and its acknowledgement (B5_SEMANTICS.md 2.3). B4 and B4b HAVE frozen cells here, so this is a hole in B5 where B4 has numbers -- not a shared absence.
  H2  after_intent_before_barrier      AUTHORITATIVE_READBACK   NOT_TESTABLE_ABSENT_IN_B5
        B5 has no 'after_intent_before_barrier' position: the worker issues an RPC and the server persists it transactionally, so there is no worker-side window between the write and its acknowledgement (B5_SEMANTICS.md 2.3). B4 and B4b HAVE frozen cells here, so this is a hole in B5 where B4 has numbers -- not a shared absence.
  H2  after_intent_before_barrier      NO_READBACK              NOT_TESTABLE_ABSENT_IN_B5
        B5 has no 'after_intent_before_barrier' position: the worker issues an RPC and the server persists it transactionally, so there is no worker-side window between the write and its acknowledgement (B5_SEMANTICS.md 2.3). B4 and B4b HAVE frozen cells here, so this is a hole in B5 where B4 has numbers -- not a shared absence.

--- H3, non-escalation ---
  HELD
    - 0 declared ambiguities across 118 non-void runs, as predicted

--- summary ---
     4  DISAGREES
     4  NOT_TESTABLE_ABSENT_IN_B5

  4 cell(s) are ABSENT IN B5, not missing from the collection. No agreement claim covers them, and the comparison is not complete.
